<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
              https://maven.apache.org/xsd/assembly-2.2.0.xsd">

    <id>lambda</id>

    <formats>
        <format>zip</format>
    </formats>

    <!-- No top-level directory inside the zip -->
    <includeBaseDirectory>false</includeBaseDirectory>

    <files>
        <!-- Native executable built by native:compile -->
        <file>
            <source>${project.build.directory}/${project.artifactId}</source>
            <destName>${project.artifactId}</destName>
            <fileMode>0755</fileMode>
        </file>

        <!-- Bootstrap entry-point; ${project.artifactId} is filtered at build time -->
        <file>
            <source>src/assembly/bootstrap</source>
            <destName>bootstrap</destName>
            <fileMode>0755</fileMode>
            <filtered>true</filtered>
        </file>
    </files>

</assembly>
